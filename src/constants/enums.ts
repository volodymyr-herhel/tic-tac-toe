export enum Language {
  English = 'en',
  Persian = 'fa',
}

export enum Difficulty {
  Easy = 'easy',
  Medium = 'medium',
  Hard = 'hard',
}

export enum GameResult {
  Win = 'win',
  Loss = 'loss',
  Draw = 'draw',
}
